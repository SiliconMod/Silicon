# ItemTransferHub

## 基本信息

| 属性 | 值 |
|------|----|
| 类名 | `ItemTransferHub` |
| 父类 | `Block` |
| 分类 | Category.distribution |
| 尺寸 | 3x3 |
| 连接范围 | 20格 |
| 最大连接数 | 20 |

## 合成配方

| 材料 | 数量 |
|------|------|
| Copper | 80 |
| Lead | 40 |
| Metaglass | 20 |
| Graphite | 30 |
| Silicon | 25 |
| Titanium | 15 |

## Block 属性

- `hasItems`: false（零缓冲，纯路由器）
- `hasPower`: true
- `consumesPower`: true
- `outputsPower`: false
- `conductivePower`: true
- `consumePower`: consumePowerDynamic（每次传输消耗10电力）
- `update`: true
- `solid`: true
- `configurable`: true
- `alwaysUnlocked`: true

## 连接机制

### 可连接的建筑类型（白名单）

`shouldConnect()` 只允许以下类型的建筑，纯物流方块（传送带、路由器、交叉器等）不可连接：

| 类别 | 具体类 | 说明 |
|------|--------|------|
| 存储 | `CoreBlock` | 核心 |
| 存储 | `StorageBlock` | 仓库/容器/两用仓库（核心旁已合并的容器排除） |
| 生产 | `GenericCrafter` / `MineConverter` / `Drill` | 工厂、矿物转换器、钻头 |
| 消耗 | `ItemTurret` | 炮塔需要弹药输入 |
| 单位生产 | `UnitFactory` / `Reconstructor` | 兵工厂、重构工厂需要原料 |
| 物流 | `ItemTransferHub` | 中枢之间手动链接 |
| 泛化 | 注册了物品消耗（itemFilter）的方块 | 自动接入；无消耗类型被排除 |

### 点击连接（电力节点式）

完全模仿 PowerNode 的链接行为：

- **单击有效建筑** → 连接/断开切换（`config(Integer.class, pos)`）
- **双击自身（无连接时）** → 自动连接范围内所有有效建筑
- **双击自身（有连接时）** → 清除所有连接
- **单击无效建筑** → 退出配置模式
- **放置时** → 自动连接范围内所有有效建筑
- 手动连接存储在 `links`（IntSeq）中
- 手动连接的建筑会加入网络拓扑

### 连接验证

`linkValid()` 检查：
1. 不是同一建筑
2. 调用方是 ItemTransferHub（防止 ClassCastException）
3. 同一队伍
4. 是可连接的建筑类型（`shouldConnect()`）
5. 在连接范围内（`connectionRange * tilesize`）

## 传输模式

### 1. 按需拉取 (Demand-Pull)
- 检查网络中每个建筑的物品需求
- BFS搜索全网最近的有该物品的供应商（**不经过欠压/禁用中枢**，无电枢辖内建筑视为不可达）
- **供源三级**：仓库 → 核心 → 矿机/工厂产出物（静态 `consumesItem` 判定：只取产出，
  **绝不抽用任何工厂/消费者的输入原料**，无兜底第四级）
- 执行proxy转移（直接操作双方inventory）

### 2. 满产推送 (Surplus-Push)
推送只面向存储（产出上行），工厂补料一律由拉取侧按消耗预算执行：
1. **核心（未满即推）**：矿机/工厂任一输出达 75% 容量即排空；核心 `acceptItem` 为真即推
   （`getMaximumAccepted` 为动态实际容量，随核心联动扩容/多核实时更新，非静态方块容量）
2. **仓库兜底**：核心满 / 拒收 / 无核时，跨全网 BFS（直连 + 跨中枢）找最近仓库强制入库
   （排除推送源自身，防自投自收）
- 仓储作为推送源：单物品 ≥90% 容量强制回运；或核心该物品 <75%（动态容量计算）时回收存量

### 网络级开关

两个模式始终启用（无 Pull/Push 切换按钮）：
- `enableDemandPull`: 按需拉取（默认true）
- `enableSurplusPush`: 满产推送（默认true）

## Proxy转移机制

零缓冲的核心实现 - 不经过hub中转，直接操作双方inventory：
```java
consumer.handleItem(supplier, item);  // 交付
supplier.items.remove(item, 1);       // 扣除
```

## BFS最近搜索

从本hub开始BFS遍历网络，找到第一个有目标物品的建筑即为最近：
- 本地（距离0）: 10电力/物品
- 1跳（距离1）: 20电力/物品
- ...

## 电力消耗（经由口径）

- 无空闲功耗：`consumePowerDynamic` 仅按实际传输计费
- **每个枢纽只统计"通过自己"的那一跳**：物品每经过一个中枢，该中枢自身消耗 10 电力、经手件数 +moved；发起枢不为其它枢的经手买单
- 路径不可达（拓扑竞态）时只按端点归属各记一跳，绝不把全程记到单个枢头上
- **瞬时请求平稳**：计费写入 30 帧环形平滑缓冲，请求 = 最近 30 帧均值（摊平 6Hz 批量突发、消除耗电锋；调度先于求值保证每笔费用恰好摊入 30 帧）
- **电力硬门控**：只有满电（status ≥ 0.999）才工作/中转；欠压立即完全停止并进入 60t 冷却；
  冷却结束先「探测」——发出真实请求但绝不搬运，请求被电网足额交付后才恢复（无源电网永不空转）
- 禁用/断电 → 瞬时请求清零（无幻影需求）、拉取/推送停止、路径选择跳过本枢；运输速率随 10s 窗口平滑降零（约 10 秒），耗电约 1 秒内归零
- **显示口径 = 实际取电量**：电力消耗**按秒计算**（最近 60 tick 滑动窗口），稳态流量下 ≈ 10 × 速率
- `conductivePower = true` → 可通过邻近电力源或电力节点供电

## 吞吐统计（运输速率）

- 统计口径 = **所有途经本枢的物品**：本枢发起的直连/跨网转移 + 其它枢纽发起、路过本枢的中转
- 远端中转件数通过 `transferCountNext` 延迟一帧并入，与延迟计费同帧同步
- **10 秒滑动窗口**（600 tick）求平均，状态栏每 10 tick 刷新一次
- 稳态流量下 耗电（每秒）≈ 10 × 速率（镜像测试场景3/4 固化）；瞬时突发时两者按各自窗口独立反映

## 绘制

### draw() - 常驻绘制
- `super.draw()` 先绘制方块贴图
- 仅在 `Renderer.laserOpacity > 0` 且非 payload 时绘制连线
- 绘制层级 `Draw.z(Layer.power)`（电力层），**画完必须恢复原层级**
- 只遍历 `links`（手动连接）；去重：Hub 对之间只画一次（`other.id >= id` 跳过）；`linkValid` 验证每个链接
- 连线样式：电力节点激光贴图（laser/laser-end，laserScale=0.25，端点内缩 1.5px 投影），稳定色不闪烁，透明度走「中枢连线透明度」设置
- **颜色按目标类型区分（lineColorFor）**：
  - Hub↔Hub：**粉色 `hubLinkColor = #ff88dd`**——网络骨架一眼可辨
  - Hub→建筑：物流色 `linkColor = Pal.items`

### drawSelect() - 选中时绘制
- 强调色虚线范围圈（电力节点式）

### drawConfigure() - 单击配置模式绘制（三色标记）
- 脉冲圆圈（自身）+ 范围圆圈
- 扫描范围内全部可连目标，与放置预览同口径：
  - **蓝 `Pal.place` = 已直连**
  - **紫 `Pal.reactorPurple` = 同网络但未直连**（跨枢链路可达；判定含中枢本身，
    A—B—C 链上单击 C 时 A 正确显示紫色——修复旧版「同网只考虑直接连接」漏判）
  - **绿 `Pal.heal` = 可新建直连（不在任何网络内）**

### drawPlace() - 放置预览绘制（三色标记 + 连线）
- 范围圈 `Pal.placing`；候选按【距离就近】模拟两阶段自动连接（中枢优先 / 网络外非中枢 / 上限截断），所见即所得：
  - **蓝 `Pal.place` 方框 + 连线 = 将连接**（连线颜色与放置结果一致：中枢间粉色、其余物流色）
  - **紫 `Pal.reactorPurple` 方框 = 即将连入的网络内成员**（coveredByHubs 整网 BFS，含跨枢覆盖；不画连线——不会直连）
  - **绿 `Pal.heal` 方框 = 可新建直连（网络外、未入选）**

### drawPlanConfigTop() - 复制配置的规划预览
- F 键拾取复制 / 原理图粘贴出的连接配置以虚线预览；
- 虚线颜色与常驻连线同口径（中枢间粉色、其余物流色），端点蓝框

## 状态栏 (Bars)

- **health**: 生命值
- **silicon-hub-power**: 电力状态（缓冲区电量 / 缓冲区容量）
- **silicon-hub-power-cost**: 每秒实际取电量（如 `Power Cost: 10.0/s`）
- **silicon-hub-connections**: 连接数（当前连接数 / 最大连接数）
- **silicon-hub-transfer-rate**: 传输速率（含途经中转，10 秒平均件/秒）

## 序列化

- 保存字段: `network.id`(int) + `links`(short 数量 + int pos...)
- 格式版本 `version() = 1`
- 读取时 `revision < 1`（未序列化链接的旧构建存档）直接跳过自定义数据，避免错位解析损坏存档流
- 存档实体按长度分块读写，格式不匹配的影响被限制在单个建筑内
- 加载保护：`updateTopology()` 在 `world.isGenerating()` 期间不因"目标不存在"剔除链接
  （加载时邻居建筑可能尚未创建），加载完成后由周期刷新兜底清理失效链接

## 暂停白名单 UI

通过 Silicon 设置 → "管理暂停白名单" 按钮打开白名单管理对话框：
- 显示当前白名单玩家列表
- 支持输入玩家名添加
- 支持点击移除按钮删除
- 多人模式下通过 `pause-grant`/`pause-revoke` 包同步到服务器
- 也可通过聊天命令管理：`!pause grant/revoke/list`

## 版本历史

| 版本 | 变更 |
|------|------|
| a0.8.1 | 初始创建（框架代码） |
| a0.8.2.0 | 完善传输逻辑：proxy转移、BFS搜索、网络开关 |
| a0.8.2.2 | 修复拓扑/可视化/国际化/按钮显示 |
| a0.8.2.3 | 改为电力节点式操作：自动扫描+点击连接+常驻连线绘制 |
| a0.8.3.0 | 连接过滤白名单（仅生产+存储）、网络内重复连接检查、电力节点式双击逻辑 |
| a0.8.3.1 | 修复双击断连 bug、删除 Pull/Push 按钮（两模式始终启用）、修复状态栏 {0}、暂停描述 |
| a0.8.3.2 | 修复连线不可见（edge-to-edge + Layer.power）、修复 pullOnDemand 不传输物品、耗电量显示实际数值、添加暂停白名单 UI |
| a0.8.4.0 | 修复 hub-to-hub 连线不显示（draw 增加 data.hubs 遍历）、修复电力系统接入（conductivePower=true + consumePower(5f)） |
| a0.8.5.0 | 添加 MineConverter 到连接白名单、linkValid 添加 ClassCastException 保护、代码健壮性全面改进 |
| a0.8.5.1 | 修复 timers 数组大小不足导致的崩溃 |
| a0.8.5.2 | 完全模仿电力节点链接行为：hub-to-hub 手动链接、移除自动发现扫描、放置时自动连接、配置模式点击无效目标退出、修复白色连线（Draw.reset 问题）、添加连接数限制显示、添加电力缓冲（50f） |
| a0.8.6.0 | 合并 PR #1 MineConverter 多人同步修复 |
| a0.8.6.1 | 性能优化：BFS 队列/visited 池化为实例字段 + IntSet 替代 Seq，移除 HubData 死代码，修复 rebuildData 双向重建，添加传输速率状态栏 |
| a0.8.7.0 | 同步上游方块搜索功能 |
| a0.8.7.1 | 修复容器类拉取（getMaximumAccepted 替代 block.itemCapacity）、修复耗电不生效（移除被 Block.consume 吞掉的 consumePowerBuffered） |
| a0.11.6.0 | 四项修复：恢复连接存档序列化（v1 格式 + 加载期防误删）；耗电显示对齐实际取电（欠载折算/禁用断电清零/延迟计费不累积）；核心满回退仓库改为跨全网 BFS 查找；运输速率统计含途经中转件数（10s 真实滑动窗口） |
| a0.11.7.0 | 复核加固：推送增加输入料保护——被堵生产建筑仍在接收的原料不外运（acceptItem 判定），消除推送→拉取乒乓倒手虚增吞吐与耗电；反编译验证 acceptItem/consumesItem/itemFilter 语义链 |
| a0.11.8.0 | 反馈修复：重构工厂（Reconstructor）纳入 isFactory 可被供料；连接白名单收紧——移除"有物品栏即连"泛化，传送带等纯物流方块不再可连；计费归属严格化为"每枢只记通过自己的一跳"；拉取/推送调度节流至 6Hz 抑制统计虚高 |
| a0.11.8.1 | i18n 审计：补齐 bar.poweramount/power/poweroutput/stat.health 兜底键 |
| a0.11.8.2 | 固化检查脚本与 Checklist/DeepCheck 文档对齐当前代码锚点 |
| a0.11.8.3 | 部署链修复：固化脚本自动同步游戏模组目录（根因：游戏目录滞留旧包致修复不生效） |
| a0.11.8.4 | 死代码清理：servedByOtherHub 系、Tile 版 overlaps、堆叠注释 |
| a0.11.9.0 | **关键修复**：计费折叠误用 += 导致 powerConsumed 跨帧无限膨胀（"工厂场景耗电异常高"深层根因，由新增镜像测试捕获）；新增 tests/HubTransferStatsTest 22 断言 |
| a0.11.9.1 | CI：Robin 审查工作流加守卫——上游上下文无 LLM secrets 时优雅跳过（修复 PR #27 review 检查必失败） |
| a0.11.10.0 | 实测回归修复六项：①中枢互连恢复（shouldConnect 的 items 守卫误排无物品栏的中枢）；②计数折叠恢复累加语义（赋值覆盖同帧自有件数→速率恒 0 而耗电正常）；③放置预览同网过滤、双击/放置统一两阶段中枢优先建链；④超速投影器等可选增幅消耗建筑纳入供料目标；⑤核心缺货时仓库存量不受 90% 阈值限制回运核心；⑥核心旁已合并仓库排除改用 linkedCore 判据 |
| a0.11.11.0 | 实测反馈五项：炮台供弹改走真实 handleItem（物品→弹药正确转换，修复"死库存"既非弹药也无法取出）；核心 <75% 即回运仓库物品（不再要求仓库 ≥90% 盈余，与产出推送阈值对齐）；最大连接数 50→20；连线改墨绿电力线风格（常驻/放置预览/规划虚线三处） |
| a0.11.12.0 | 补货节奏门控：非炮台消费者按 30tick（0.5s）最小间隔供料——修复"批量一次补满导致搬运量 ≈ 消耗量×批次粒度、耗电与速率远超产线真实需求"；updateTopology 时清理失效消费者的节奏记录 |
| a0.11.13.0 | 连线样式改原版电力节点激光（laser/laser-end 贴图 + Pal.items 连接bar同色脉动），常驻与放置预览一致 |
| a0.11.14.0 | 激光尺寸/端点对齐原版：laserScale=0.25（此前误用简化重载默认 1）、端点内缩 1.5px；透明度确认遵循 Renderer.laserOpacity（与原版电力连接线同一设置） |
| a0.11.14.0 | 激光尺寸/端点对齐原版：laserScale=0.25（此前误用简化重载默认 1）、端点内缩 1.5px；透明度确认遵循 Renderer.laserOpacity（与原版电力连接线同一设置） |
| a0.11.15.0 | **消耗匹配补货**：非炮台消费者按「快照以来真实消耗量+2缓冲」供给，从数学上封顶吞吐（替代节奏门控）；Silicon 设置新增「中枢连线透明度」滑杆（hubLinkOpacity，10-100%），中枢三处绘制改用此设置 |
| a0.11.15.1 | 新增中枢流量调试日志开关（排查吞吐异常） |
| a0.11.15.2/3 | 版本号对齐；忽略分析转储文件（chore） |
| a0.11.16.0 | 连线去除呼吸闪烁改为稳定色；透明度滑杆下限放宽至 0%（可完全隐藏连线） |
| a0.11.17.0 | **推送链路与电力语义重构**：①矿机/工厂产物核心未满即推（其次仓库），推送不再直接分发消费者，工厂补料统一由拉取按消耗预算执行；②输入料保护改 consumesItem 静态配方判定——修复满仓时 acceptItem 变假导致的仓库↔工厂乒乓空转（"仅仓库-中枢-窑炉速率 474、窑炉不工作"根因）；③75% 核心阈值全部改用 getMaximumAccepted 动态实际容量（随联动扩容实时更新）；④电力硬门控：status<0.999 完全停止 + 60t 冷却 + 供电探测恢复，BFS 路径/端点全面过滤欠压中枢；⑤计费平滑缓冲摊平瞬时请求（电网侧无跳变）+ 耗电与速率共用 10s 滑动窗口（严格 10:1）；⑥仓库落点自排除防自投自收 |
| a0.11.18.0 | **供源纪律收紧**：拉取供源删去第四级兜底，pass2 改静态 `consumesItem` 判定——任何工厂/消费者的输入原料绝不外抽（无论槽位是否已满），修复"拿工厂原料做供给导致被抽工厂断粮、中枢只连一个工厂时无法正常运作"；贴面保护豁免存储供源（修复贴面仓库喂不到旁边工厂） |
| a0.11.18.2 | 连线端点对齐原版 PowerNode.drawLaser 公式（半边长先内缩 1.5px 再投影）——修复旧写法先到边界再外推 1.5px 导致端点悬在方块外；常驻连线与放置预览两处同步修正 |
| a0.11.19.0 | **放置预览双色标记**：预览模拟两阶段自动连接（中枢优先/同网排除/上限截断），实际会放下的连接画蓝色方框+连线；其余符合连接标准但不会连接的候选（已被其它中枢网络覆盖、超出连接数）画绿色方框（与单击配置显示同色），不再隐藏也不再误画连线 |
| a0.11.20.0 | **默认连接改距离就近优先**（覆盖放置预览/放置/双击三处）：候选按与新建中枢的距离升序排序后依次建链，同网排除与连接数上限不变；预览以同一排序口径模拟，所见即所得 |
| a0.11.20.1 | **同网排除收紧**：非中枢建筑已在任何同队中枢网络内（含即将连入的中枢网络、以及范围外邻近中枢经跨枢链路覆盖的建筑）一律不直连；范围内的中枢仍然全部连接。排除判定统一走 collectNearbyHubCoverage（2 倍连接范围内全部同队中枢的整网 BFS），预览/放置/双击三处同口径 |
| a0.11.21.0 | **统计与连接顺序调整**：①电力消耗按秒计算（60 tick 滑动窗口），运输速率保持 10 秒窗口，稳态下 耗电 ≈ 10×速率；②自动连接两阶段均就近——优先连接范围内全部中枢，其次连接已选中枢网络之外的非中枢建筑（预览/放置/双击同口径） |
| a0.11.22.0 | **核心满防焚烧 + 耗电锋消除 + 用途介绍重写**：①coreIncinerates 规则下 getMaximumAccepted 膨胀为 10.7 亿导致"核心已满仍运送→物品被焚烧丢弃、不入仓库"——推送目标与 75% 回收阈值全部改用真实容量 storageCapacity（coreHasRoomFor），directTransfer 对核心收方复核余量；②计费平滑窗口 10→30 帧，耗电峰值降至 1/3（守恒不变）；③修复 draw() 设置 Layer.power 后不恢复层级导致的随机渲染顺序异常；④中英双语用途介绍全面重写（富文本分节排版） |
| a0.11.22.1 | **两处预期外行为修正**：①关闭原版 copyConfig——F 键拾取复制会把整组连接配置带到异地新枢，产生预期外链路；②断电时运输速率不再瞬间清零，改为随 10 秒窗口推入零桶平滑衰减（耗电约 1 秒内自然归零），镜像测试新增衰减场景（31 断言） |
| a0.11.22.2 | **恢复连接可复制**（撤回 copyConfig=false）：实现 config() 返回以自身为原点的相对坐标 Point2[]——F 键拾取复制后异地放置时由 placeEnded 按相对偏移 + linkValid 现场校验，只连得上合法目标；另修复 mod.hjson 被 PS 5.1 Set-Content 写入 BOM 导致模组不加载的问题（已知坑已固化流程） |
| a0.11.0.0 | **三色标记统一 + 中枢间粉色连线**：①放置预览与单击配置同口径三色——蓝=已/将直连、紫=网络内（预览时=即将连入的网络，整网 BFS 含跨枢覆盖）、绿=可新建（网络外）；②修复 inSameNetwork 漏判跨枢中枢本身（A—B—C 链单击 C 时 A 误显绿色）——同网判定补 cur==b/nb==b；③连线颜色按目标类型区分：中枢↔中枢粉色 #ff88dd、中枢→建筑物流色，常驻连线/放置预览/复制规划虚线三处共用 lineColorFor；④反编译验证 v159 复制链路（PlacementFragment Binding.pick → copyConfig=true 读 build.config() → 写 lastConfig → ConstructBuild.finish 经 configured() 自动应用相对坐标 + placeEnded 兜底） |
| a0.11.0.0 | **复制对齐电力节点（重构）**：①`saveConfig=false` 回归电力节点同款——此前 true 时 Block.nextConfig() 把旧 Point2[] 粘到后续每次拖线/单击放置（莫名虚线+预期外链接根源）；②连接预览挂原版同款两钩子：drawPlanConfig（悬停幽灵：DesktopInput 每帧附加 lastConfig 后调用并立即清空）+ drawPlanConfigTop（入队/原理图粘贴/拖线，成批粘贴时画出计划间粉色激光网）；③已放置目标加距离校验（新位置范围内真正可连才画，与实际链接口径一致）；④层级 Layer.plans-5（85→80）：规划幽灵之下、电力线之上；⑤planConfigClipSize 重写防预览被相机裁剪；⑥placeEnded 应用配置后一次性消费清空 lastConfig；⑦反编译依据：PowerNode lambda$new$1（Point2[] 经 Integer handler 重放到绝对坐标）、lambda$static$26（planFinder 仅计划间）、Block.nextConfig/saveConfig 闸门、DesktopInput drawBottom 的 bplan.config 无条件附加路径 |
| a0.11.0.0 | **复制放置四项修复**：①连接单一归属裁决——新增 `stealFromOtherHubs`，认领路径（Point2[] 复制配置 / Integer 单击·双击自动连·挂起补连）统一把普通目标从其它全部中枢的 links/hubLinks/pendingLinks 抢回，杜绝「原有周围的中枢与复制枢同拉一个建筑」的重复/竞争连接（复制放置重复连接根因）；②复制放置不再自由补连——placeEnded 检测显式 Point2[] 配置或 configured 已写入任意连接/挂起即跳过 autoConnectNearby，拓扑完全由复制模式 + 挂起队列决定（所见即所得），并消除补连占用 maxConnections 配额饿死偏后建成挂起目标的漏连根因；③复制预览连线层级显式抬升至 `max(当前层+1, Layer.plans+4)`——同层绘制按插入序覆盖，先画的中枢连线会被后画的兄弟计划幽灵盖住（「拖动时被其它建筑遮挡」根因；v159 反编译确认悬停路径 z=85、拖线路径 z=120、常驻连线 z=88）；④挂起链接过期 180 秒 → 600 秒（大蓝图/慢速建造静默漏连防护） |
| a0.11.0.0 | **全局连线覆盖层 + 放置侧预览 + 满连接点击防剥离**：①常驻连线从「随方块自身 draw() 绘制」改为 `Trigger.drawOver` 全局覆盖层——世界渲染收尾后一次性遍历 allHubs 按 `Layer.plans+3` 统一绘制（电力节点家族式上层绘画，全部更新后绘画，严格位于一切方块几何之上，带视口裁剪）；②新增放置侧连接预览：放置任意可连建筑时，范围内最近的中枢（与建造完成事件同口径）以实线激光连向幽灵位置 + 幽灵蓝框 + 该枢范围虚线圈，无中枢覆盖不标记＝放下不会自动接入；判定用 HubRouting.shouldConnectBlock 方块级白名单；③修复满连接中枢点击同网/他网建筑会把目标从原中枢剥离成孤儿链接的回归——容量闸门先于单一归属裁决，满员时点击一律不动 |
| a0.11.0.0 | **预览同口径三修正**：①放置侧预览与实际连接完全对齐——位置已有归属不标记、胜者=最近可连枢（满员不跳次近，与事件一致）、胜者满员（≥maxConnections）不标记（修复「预览连到满员中枢但放下接不上」）；②复制/原理图预览层级固定抬升 `Layer.overlayUI`(120)——高于一切世界几何（方块≤40/连线88/子弹100/特效110/飞行单位115），任何后绘制精灵都无法遮挡；调试日志改记实际绘制 z 与环境 z（旧日志打印钩子环境 z=90/110 造成「未改层」误判）；说明：复制预览必须留在原版输入钩子内（只有此处能取到同批计划上下文，全局覆盖层拿不到拖动/悬停中的计划），常驻连线才是覆盖层；③复制放置恢复「自动连接范围内全部中枢」（autoConnectNearbyHubs 粉色骨架、无上限、双向对称、距离就近、不占普通配额），普通建筑拓扑仍保持复制原模式 |
| a0.11.0.0 | **覆盖层挂点修正 drawOver→postDraw**：v159 反编译证实 v8 渲染顺序为 …→fire(drawOver)→blocks.drawBlocks()→…→Draw.flush/sort(false)→fire(postDraw)——v8 方块延迟渲染，drawOver 时方块尚未上屏，其缓存绘制先 flush 队列再覆盖，导致 drawOver 中绘制的常驻连线被全体方块盖住（实测「非预览中枢连接全部不可见」）。改挂 `Trigger.postDraw`（最终 flush 与 sort(false) 之后）：此刻绘制必为一帧画布最后一笔，位于一切世界几何之上；绘制后立即 `Draw.flush()` 落屏防投影切换丢帧；放置侧连接预览同挂点同步迁移 |
| a0.11.0.0 | **F 复制预览防遮挡 + 偶数方块锚点 + 满员回退**：①复制/原理图预览改为「钩子期解析线段 + postDraw 统一渲染」——输入阶段提交的精灵会被其后的方块缓存绘制覆盖（同 drawOver 教训），现 drawPlanConfig/drawPlanConfigTop 只产出 {x1,y1,x2,y2,粉色?} 线段入 pendingCopyLinks 队列，postDraw 按 `Layer.overlayUI`(120) 一次画完并清空，F 键拾取移动/原理图粘贴全程不再被任何方块覆盖；②放置侧预览锚点对齐原版 `InputHandler.tileX/tileY`：鼠标世界坐标先减 `block.offset` 再取整（反编译证实 offset=(size+1)%2*4，偶数方块半格），修复偶数方块预览偏离一格；③建造完成事件与放置预览统一「满员回退」：最近可连枢普通配额满 → 跳过并向更远的范围内枢寻找空位（此前直接不连/不标记），中枢间目标无上限不受限 |
